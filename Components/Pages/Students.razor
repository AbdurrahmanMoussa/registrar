@page "/"
@rendermode InteractiveServer
@using Registrar
@inject NavigationManager Navigator
<PageTitle>Students</PageTitle>
<main class="m-5">
	<header class="d-flex justify-content-between">
		<h3>Students</h3>
		<a @onclick="NavigateToAddStudent" class="align-content-end btn btn-primary rounded-1">+</a>
	</header>

	<section class="d-flex">

		<table class="table table-hover align-content-center border-5">
			<thead>
				<tr class="student-table">
					<th>id</th>
					<th>Last Name</th>
					<th>First Name</th>
					<th>Type</th>
					<th>Action</th>
				</tr>
			</thead>
			<tbody>
				@foreach (var student in students) 
				{
					<tr>
						<td>@student.Id</td>
						<td>@student.LastName</td>
						<td>@student.FirstName</td>
						<td>@student.Type</td>
						<td><button @onclick="()=>RegisterStudent(student.Id)" class="border-0 btn-primary color-white rounded-1">Register</button></td>
					</tr>
				}
			</tbody>
		</table>
	</section>
</main>
@code {
	//TODO: use try catch to check for null students here before assigning
	List<Student> students = Helper.InitialStudents;

	private void RegisterStudent(int studentId)
	{
		Navigator.NavigateTo($"/register/{studentId}");
	}
	private void NavigateToAddStudent()
	{
		Navigator.NavigateTo("addstudent");
	}

}
